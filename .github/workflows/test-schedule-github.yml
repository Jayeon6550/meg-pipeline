name: Test Schedule GitHub Runner

on:
    schedule:
        - cron: "*/5 * * * *"
    workflow_dispatch:

jobs:
    test-scheduler-github:
        runs-on: ubuntu-latest
        steps:
            - name: Echo testing message
              run: echo "Scheduler is working on GitHub Runner!"
